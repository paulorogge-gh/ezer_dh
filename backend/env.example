PORT_API=8000
# Em produção use host do banco no Azure MySQL (nunca localhost)
DB_HOST=<seu_host_azure_mysql>
DB_PORT=3306
DB_USER=<seu_usuario>
DB_PASSWORD=<sua_senha>
DB_NAME=ezer_dh
# Habilite SSL quando exigido pelo provedor
DB_SSL=true
JWT_SECRET=ezer_dh_secret_key_2025
JWT_EXPIRES_IN=24h
# Origem única (compatibilidade)
CORS_ORIGIN=http://localhost:8080
# Múltiplas origens permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:8080,http://server01.paulorogge.com.br,https://server01.paulorogge.com.br

# Integração com Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING=<sua_connection_string_do_storage>
