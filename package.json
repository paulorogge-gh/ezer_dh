{
  "name": "ezer-dh",
  "version": "1.0.0",
  "description": "Plataforma de gestão de pessoas e desenvolvimento humano",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "bash -c 'cd backend && PORT_API=3001 npm start'",
    "dev": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start:all": "bash -c 'cd backend && PORT_API=3001 npm start'",
    "start:azure": "bash -c 'cd backend && (npm ci --omit=dev || npm install --omit=dev) && FRONTEND_PUBLIC_DIR=../frontend/public PORT_API=3001 node src/app.js'",
    "test": "jest",
    "build": "echo 'No build step for root (using runtime build)'",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "setup": "node scripts/initial.js",
    "logs": "cd backend && npm run logs"
  },
  "keywords": [
    "gestao-pessoas",
    "desenvolvimento-humano",
    "avaliacao-desempenho",
    "rh"
  ],
  "author": "Ezer Desenvolvimento Humano",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "jest": "^29.6.2"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
